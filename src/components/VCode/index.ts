import TDialog from './src/index.vue'

// (TDialog as any).install = function(Vue) {
//   Vue.component(TDialog.name, TDialog)
// }

// TDialog.install = function(Vue) {
//   Vue.component(TDialog.name, TDialog)
// }

export default TDialog
